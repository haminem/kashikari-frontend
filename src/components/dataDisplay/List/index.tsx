import React from "react";

export type ListProps = {
  height?: string;
  width?: string;
  children?: React.ReactNode;
};

function List({ height, width, children }: ListProps) {
  return <></>;
}

export default List;
